# Example .env file for configuration (do not use this for production)

# The server address (replace with actual server address)
SERVER="your_server_address_here"

# The port the server listens on (replace with actual port)
PORT="8984"

# API Token for authentication (replace with your actual token)
API_TOKEN="your_api_token_here"

# Endpoint for tickets API (replace with actual endpoint)
ENDPOINT_TICKET="api/tickets"
